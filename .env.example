# Supabase transaction pooler (port 6543) — used by the app at runtime.
DATABASE_URL=

# Supabase direct connection (port 5432) — used by the migration and seed scripts.
DIRECT_URL=

# 32+ random bytes, HS256 signing key for the session cookie.
JWT_SECRET=
